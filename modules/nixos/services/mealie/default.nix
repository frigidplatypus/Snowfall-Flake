{
  lib,
  config,
  options,
  pkgs,
  ...
}:

# Mealie module moved to the recipes system config.
# This stub remains to avoid accidental breakage for imports that reference
# the module path. The actual configuration now lives in
# `systems/x86_64-linux/recipes/default.nix`.

{
}
